pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Example checkout step
                checkout scm
            }
        }
        stage('Build') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
